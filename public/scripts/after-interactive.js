// afterInteractive script - loads after page becomes interactive
console.log('[SCRIPT] afterInteractive loaded at:', performance.now().toFixed(2), 'ms');
window.afterInteractiveLoaded = performance.now();
